{
    IBClasses = (
        {CLASS = DodoController; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                addURL = id; 
                chooseFiles = id; 
                generate = id; 
                replaceFile = id; 
                toggleConsoleDrawer = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}